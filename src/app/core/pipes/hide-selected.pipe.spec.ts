import { HideSelectedPipe } from './hide-selected.pipe';

describe('HideSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new HideSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
